const { authRouter } = require("./authRouters");
const { contactsRouter } = require("./contactsRouters");
const { filesRouter } = require("./filesRouter");

module.exports = { authRouter, contactsRouter, filesRouter };